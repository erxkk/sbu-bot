namespace SbuBot.Commands
{
    public static class SbuCommandUtility
    {
        // menu type with nav bar
        // back | forward | dropdown of choices | exit
        // preserve history like in browser
        // content depending on if looking at command or module
    }
}